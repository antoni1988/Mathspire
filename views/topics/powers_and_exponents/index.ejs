<%
const introduction = `
  <p>
    A power (or exponent) shows how many times a number is multiplied by itself.
  </p>
  <div class="text-center my-3">
    <strong>x<sup>n</sup> = x × x × ... × x</strong> (n times)
  </div>
`;

const image = {
  src: "/images/topics/powers/example.png",
  alt: "Powers Example"
};

const content = `
  <h3 class="mt-4">Basic Rules</h3>
  <div class="accordion" id="powerRules">
    <div class="accordion-item">
      <h4 class="accordion-header">
        <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#productRule">
          Product Rule
        </button>
      </h4>
      <div id="productRule" class="accordion-collapse collapse show">
        <div class="accordion-body">
          <p>x<sup>m</sup> × x<sup>n</sup> = x<sup>m+n</sup></p>
        </div>
      </div>
    </div>
    <div class="accordion-item">
      <h4 class="accordion-header">
        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#quotientRule">
          Quotient Rule
        </button>
      </h4>
      <div id="quotientRule" class="accordion-collapse collapse">
        <div class="accordion-body">
          <p>x<sup>m</sup> ÷ x<sup>n</sup> = x<sup>m-n</sup></p>
        </div>
      </div>
    </div>
  </div>
`;
%>

<%- include('../../components/topic_page', {
  topic,
  prerequisites,
  exerciseGroups,
  introduction,
  image,
  content
}) %>